# 测试执行器系统架构文档

## 1. 系统概述

测试执行器（TestExecutor）是一个用于自动化测试的桌面应用程序，支持多种测试工具（CANoe、TSMaster、TTworkbench）的测试用例执行管理。该系统采用分层架构设计，支持任务调度、配置管理、用例映射、执行监控等功能。

### 1.1 技术栈

| 层级 | 技术/框架 |
|------|-----------|
| 桌面框架 | PyWebView + Flask |
| Web框架 | Flask + Jinja2模板 |
| 后端语言 | Python 3.x |
| 前端 | HTML5 + CSS3 + JavaScript |
| 打包工具 | PyInstaller |

---

## 2. 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              应用层 (app.py)                                    │
│                     PyWebView 桌面应用入口 + Flask Web服务                       │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              API层 (api/)                                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ task_api │ │ config_  │ │ case_    │ │ env_api  │ │func_test_│          │
│  │          │ │   api    │ │mapping_  │ │          │ │   api    │          │ ...更多
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           核心业务层 (core/)                                   │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐              │
│  │  task_executor  │ │ execution_plan / │ │  case_mapping_  │              │
│  │ _production.py  │ │ task_compiler.py │ │    manager.py   │              │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘              │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐              │
│  │ task_scheduler   │ │   env_checker    │ │ execution_      │              │
│  │                  │ │                  │ │ observability   │              │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘              │
│                        适配器层 (core/adapters/)                              │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐              │
│  │  canoe_adapter   │ │ tsmaster_adapter │ │ttworkbench_     │              │
│  │                  │ │                  │ │    adapter      │              │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘              │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            数据模型层 (models/)                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐                       │
│  │ task.py  │ │ case_    │ │ result.  │ │ executor │                       │
│  │          │ │mapping.py│ │   py     │ │ _task.py │                       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘                       │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            配置与工具层                                        │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐              │
│  │ unified_config   │ │   utils/logger    │ │  utils/report_   │              │
│  │ (single source)  │ │                   │ │    client       │              │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘              │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            前端展示层 (web/)                                   │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐                         │
│  │ templates/   │ │  static/css/ │ │  static/js/  │                         │
│  └──────────────┘ └──────────────┘ └──────────────┘                         │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 目录结构

```
python_executor/
├── app.py                          # 桌面应用入口 (PyWebView)
├── main.py                         # Web服务入口
├── main_production.py              # 生产环境入口
├── config.json                     # 配置文件
│
├── api/                            # API层
│   ├── __init__.py
│   ├── routes.py                   # 路由注册
│   ├── task_api.py                 # 任务管理API
│   ├── task_executor.py            # 任务执行API
│   ├── task_log_api.py             # 任务日志API
│   ├── config_api.py               # 配置管理API
│   ├── case_mapping_api.py         # 用例映射API
│   ├── env_api.py                  # 环境检测API
│   ├── functional_test_api.py       # 功能测试API
│   ├── service_api.py               # 服务配置API
│   └── docs_api.py                  # 接口文档API
│
├── core/                           # 核心业务层
│   ├── __init__.py
│   ├── task_executor.py            # 任务执行器(开发版)
│   ├── task_executor_production.py  # 任务执行器(生产版)
│   ├── execution_plan.py           # 内部执行模型
│   ├── task_compiler.py            # 平台任务编译器
│   ├── execution_observability.py  # 生命周期/业务健康观测
│   ├── task_scheduler.py           # 任务调度器
│   ├── task_store.py               # 任务存储
│   ├── config_manager.py           # 配置管理器(TestConfigManager)
│   ├── config_cache_manager.py      # 配置缓存管理器
│   ├── case_mapping_manager.py      # 用例映射管理器
│   ├── env_checker.py              # 环境检测器
│   ├── status_monitor.py           # 状态监控器
│   ├── result_collector.py          # 结果收集器
│   ├── variable_cache.py            # 变量缓存
│   ├── realtime_logger.py          # 实时日志
│   ├── software_validator.py       # 软件验证器
│   ├── state_machine.py            # 状态机
│   ├── state_machine_executor.py   # 状态机执行器（兼容入口，内部支持ExecutionPlan）
│   ├── test_state_handlers.py      # 测试状态处理器
│   ├── canoe_controller.py         # CANoe控制器(旧)
│   ├── tsmaster_controller.py     # TSMaster控制器(旧)
│   ├── functional_test_runner.py   # 功能测试运行器
│   │
│   └── adapters/                   # 适配器层(Adapter模式)
│       ├── __init__.py
│       ├── adapter_factory.py      # 适配器工厂真源
│       ├── adapter_wrapper.py       # 分层能力包装器
│       ├── base_adapter.py          # 适配器基类
│       ├── canoe_adapter.py        # CANoe适配器
│       ├── tsmaster_adapter.py     # TSMaster适配器
│       ├── ttworkbench_adapter.py  # TTworkbench适配器
│       │
│       ├── canoe/                  # CANoe适配器模块
│       │   ├── __init__.py
│       │   ├── adapter.py          # CANoe适配器主类
│       │   ├── com_wrapper.py      # COM接口包装器
│       │   └── test_engine.py      # 测试执行引擎
│       │
│       └── tsmaster/               # TSMaster适配器模块
│           ├── __init__.py
│           └── rpc_client.py        # RPC客户端
│
├── models/                         # 数据模型层
│   ├── __init__.py
│   ├── task.py                    # 任务和用例模型
│   ├── case_mapping.py            # 用例映射模型
│   ├── result.py                  # 结果模型
│   ├── task_log.py                # 任务日志模型
│   ├── env_check_log.py           # 环境检查日志模型
│   └── executor_task.py            # 执行任务模型
│
├── config/                        # 配置模块
│   ├── __init__.py
│   ├── settings.py                # 统一配置 facade
│   ├── config_manager.py          # 统一配置 facade
│   └── unified_config.py          # 统一配置真源
│
├── utils/                         # 工具模块
│   ├── __init__.py
│   ├── logger.py                  # 日志工具
│   ├── exceptions.py              # 自定义异常
│   ├── validators.py              # 输入验证器
│   ├── retry.py                   # 重试机制
│   ├── metrics.py                 # 性能指标
│   ├── report_client.py            # 上报客户端
│   └── excel_parser.py            # Excel解析器
│
├── web/                           # Web前端
│   ├── __init__.py
│   ├── server.py                  # Flask应用创建
│   │
│   ├── templates/                 # HTML模板
│   │   ├── base.html             # 基础模板
│   │   ├── dashboard.html        # 仪表盘
│   │   ├── tasks.html            # 任务管理
│   │   ├── config.html           # 配置页面
│   │   ├── logs.html             # 日志页面
│   │   ├── case_mapping.html     # 用例映射管理
│   │   ├── env_check.html        # 环境检测
│   │   ├── functional_test.html  # 功能测试
│   │   ├── service_config.html   # 服务配置
│   │   ├── report_config.html   # 上报配置
│   │   └── api_docs.html         # API文档
│   │
│   ├── static/                    # 静态资源
│   │   ├── css/
│   │   │   └── style.css
│   │   └── js/
│   │       └── tsmaster_steps.js
│   │
│   └── routes/                    # 前端路由
│       └── __init__.py
│
├── data/                          # 数据存储
│   └── case_mappings.json         # 用例映射数据
│
└── pkg/                           # 打包配置
    ├── __init__.py
    ├── build.py
    └── version.txt
```

---

## 4. 核心模块详解

### 4.1 应用入口层 (app.py)

**职责**: 启动桌面应用，整合Flask服务与PyWebView界面

**核心类**:
```python
class TestExecutorApp:
    def start_server(self)      # 启动Flask服务器
    def create_window(self)    # 创建桌面窗口
    def on_closed(self)        # 窗口关闭回调
    def run(self)              # 运行应用
```

---

### 4.2 API层 (api/)

| 模块 | 职责 |
|------|------|
| **task_api.py** | 任务CRUD、状态查询 |
| **task_executor.py** | 触发任务执行、获取执行结果 |
| **case_mapping_api.py** | 用例映射管理(增删改查、导入导出) |
| **config_api.py** | 系统配置读取/更新 |
| **env_api.py** | 环境检测(软件安装检查) |
| **functional_test_api.py** | 功能测试接口 |
| **service_api.py** | 服务配置接口 |
| **docs_api.py** | API文档接口 |

---

### 4.3 核心业务层 (core/)

#### 4.3.1 任务执行器 (task_executor_production.py)

**职责**: 任务执行的核心引擎，支持生产环境的熔断、重试、性能监控等功能

**当前核心流程**:
```
1. 接收平台任务 / HTTP任务
2. 通过 `TaskCompiler` 编译为内部 `ExecutionPlan`
3. 根据 `ExecutionPlan` 解析配置来源、工具类型和执行用例
4. 选择对应 `ExecutionStrategy`
5. 通过 strategy + adapter capability 执行测试
6. 收集结果并上报
7. 通过 execution observability 刷新生命周期和业务摘要
```

**核心方法**:
```python
class TaskExecutorProduction:
    def execute_plan(plan: ExecutionPlan)       # 提交内部执行计划
    def _execute_task_production(task: ExecutionPlan)     # 生产环境任务执行
    def _connect_tool_with_retry(task)         # 重试连接工具
    def _load_configuration_by_path(path)       # 加载配置文件
```

说明：
- `execute_task()` 仍保留为兼容入口，但内部会先转换为 `ExecutionPlan`
- HTTP API 与 WebSocket 入口都已经收敛到 `TaskCompiler -> ExecutionPlan`

#### 4.3.2 用例映射管理器 (case_mapping_manager.py)

**职责**: 管理接口用例名称与脚本Case编号的映射关系

**核心方法**:
```python
class CaseMappingManager:
    def register_mapping(mapping, changed_by, reason)     # 注册映射
    def get_mapping(case_no)                             # 通过编号查询
    def get_mapping_by_name(name)                         # 通过名称模糊查询
    def update_case(case_no, updates, changed_by, reason) # 更新映射
    def delete_case(case_no, changed_by, reason)          # 删除映射
    def import_mappings(data, overwrite)                  # 批量导入
    def export_mappings()                                 # 导出演射
```

#### 4.3.3 配置管理器 (config_manager.py - TestConfigManager)

**职责**: 管理测试配置文件，生成ini文件

**核心方法**:
```python
class TestConfigManager:
    def prepare_config_for_task(task)                       # 为任务准备配置
    def prepare_config_for_case(case_no, script_path, ini_config)  # 为单个用例准备配置
    def _generate_ini_from_case_config(case_no, ini_config) # 从用例映射生成ini
    def cache_config(config_name, source_path)             # 缓存配置文件
```

#### 4.3.4 任务调度器 (task_scheduler.py)

**职责**: 调度任务的执行，管理任务队列

---

### 4.4 适配器层 (core/adapters/)

#### 4.4.1 适配器模式架构

```
┌─────────────────────────────────────┐
│         BaseTestAdapter             │  (抽象基类)
│  - connect()                        │
│  - disconnect()                     │
│  - load_configuration()             │
│  - start_test() / stop_test()       │
│  - execute_test_item()               │
└─────────────────────────────────────┘
           ▲          ▲          ▲
           │          │          │
    ┌──────┴──┐ ┌────┴────┐ ┌───┴─────┐
    │ CANoe   │ │TSMaster │ │TTWorkbench│
    │ Adapter │ │ Adapter │ │ Adapter  │
    └─────────┘ └─────────┘ └──────────┘
```

#### 4.4.2 适配器工厂 (adapter_factory.py)

**职责**: 统一创建和管理测试工具适配器；当前 `adapter_factory.py` 是唯一真源

```python
class AdapterFactory:
    @classmethod
    def create_adapter(tool_type: TestToolType | str, config: dict, singleton: bool) -> BaseAdapter
    @classmethod
    def create_adapter_with_wrapper(tool_type: TestToolType | str, config: dict, singleton: bool) -> AdapterWrapper
```

补充：
- `core.adapters` 包级导出是兼容层，内部统一转发到 `adapter_factory.py`
- `TaskExecutorProduction` 通过 `ExecutionStrategySelector` 选择 `CANoeExecutionStrategy` / `TSMasterExecutionStrategy`
- `AdapterWrapper` 已退化为薄兼容层，不再承载 CANoe 配置驱动与系统变量执行语义
- `create_adapter_with_wrapper` 仅保留为显式兼容 helper，不属于默认工厂入口

#### 4.4.3 CANoe适配器 (canoe/adapter.py)

**职责**: 基于COM接口控制CANoe应用

**核心功能**:
- CANoe应用程序控制(连接/断开)
- 配置文件加载(.cfg)
- 测量控制(启动/停止)
- 信号读写(CAN/LIN/Ethernet)
- TestModule 直接执行

---

### 4.5 数据模型层 (models/)

#### 4.5.1 用例映射模型 (case_mapping.py)

```python
@dataclass
class CaseMapping:
    case_no: str              # 脚本Case编号 (如 "CANOE-001")
    case_name: str            # 接口用例名称
    category: str             # 分类 (canoe/tsmaster/system)
    module: str               # 模块名称
    script_path: str          # cfg工程文件路径
    ini_config: str           # ini文件内容配置 (JSON格式)
    enabled: bool             # 是否启用
    priority: int             # 优先级
    tags: List[str]           # 标签列表
    version: str              # 版本号
    description: str         # 描述
    created_at: str           # 创建时间
    updated_at: str           # 更新时间
```

#### 4.5.2 外部任务模型 (task.py)

```python
@dataclass
class Task:
    projectNo: str            # 项目编号
    taskNo: str              # 任务编号
    taskName: str            # 任务名称
    caseList: List[Case]     # 用例集合
    deviceId: str             # 设备ID
    toolType: str             # 测试工具类型 (canoe/tsmaster/ttworkbench)
    configPath: str           # 配置文件路径
    timeout: int             # 超时时间
    configName: str           # 配置名称
    variables: Dict           # 测试变量值字典
    baseConfigDir: str       # 基础配置目录
    canoeNamespace: str       # CANoe命名空间
```

#### 4.5.3 内部执行模型 (execution_plan.py)

```python
@dataclass
class ExecutionPlan:
    task_no: str
    project_no: str
    task_name: str
    device_id: str
    tool_type: str
    cases: List[PlannedCase]
    config_path: str | None
    config_name: str | None
    base_config_dir: str | None
    variables: Dict
    timeout_seconds: int
```

说明：
- `models.task.Task` 仍保留为外部协议模型
- 进入执行内核后，统一使用 `ExecutionPlan`

```python
@dataclass
class Case:
    caseNo: str              # 用例编号
    caseName: str             # 用例名称
    caseType: str             # 用例类型
    moduleLevel1/2/3: str    # 模块层级
    priority: str             # 优先级
    preCondition: str         # 前置条件
    stepDescription: str      # 步骤描述
    expectedResult: str       # 预期结果
    maintainer: str           # 维护人
    dtcInfo: str              # DTC信息
    params: Dict             # 测试参数
    repeat: int               # 重复执行次数
```

#### 4.5.3 结果模型 (result.py)

```python
@dataclass
class CaseResult:
    caseNo: str              # 用例编号
    result: str              # 结果 (PASS/FAIL/BLOCK/SKIP)
    remark: str              # 备注
    created: str             # 测试执行时间
    reportPath: str          # 报告地址

@dataclass
class ExecutionResult:
    taskNo: str              # 任务编号
    caseList: List[CaseResult]  # 用例结果集合
    platform: str            # 平台名称
    startTime: datetime      # 开始时间
    endTime: datetime        # 结束时间
```

---

### 4.6 配置层 (config/)

#### 4.6.1 统一配置 (unified_config.py)

```python
DEFAULT_CONFIG = {
    "server": { "websocket_url": "ws://localhost:8080/ws/executor", ... },
    "http": { "port": 8180, "host": "0.0.0.0", ... },
    "device": { "device_id": "DEVICE_001", "device_name": "测试执行设备-01", ... },
    "software": {
        "canoe_path": "C:\\Program Files\\Vector\\CANoe 17\\Exec64\\CANoe64.exe",
        "tsmaster_path": "C:\\Program Files\\TSMaster",
        "ttman_path": "C:\\Spirent\\TTman.bat",
        "workspace_path": "C:\\TestWorkspace"
    },
    "logging": { "level": "INFO", "log_dir": "logs", ... },
    "execution": { "default_timeout": 3600, "auto_start": True, ... },
    "report": { "enabled": False, "api_url": "", ... },
    "config_cache": { "enabled": True, "cache_dir": "workspace/cache/configs", ... }
}
```

说明：
- `config.json` 是唯一配置文件入口
- `settings.py` 和 `config_manager.py` 现在只是 facade，不再各自维护一套实现

---

### 4.7 工具层 (utils/)

| 模块 | 职责 |
|------|------|
| **logger.py** | 日志记录，支持文件和控制台输出 |
| **exceptions.py** | 自定义异常类 |
| **validators.py** | 输入验证器 |
| **retry.py** | 重试机制和熔断器 |
| **metrics.py** | 性能指标收集 |
| **report_client.py** | 测试结果上报客户端 |
| **excel_parser.py** | Excel文件解析(用于导入用例映射) |

---

### 4.8 前端层 (web/)

#### 4.8.1 页面路由

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | dashboard.html | 仪表盘 |
| `/tasks` | tasks.html | 任务管理 |
| `/config` | config.html | 系统配置 |
| `/logs` | logs.html | 日志查看 |
| `/case-mapping` | case_mapping.html | 用例映射管理 |
| `/env-check` | env_check.html | 环境检测 |
| `/functional-test` | functional_test.html | 功能测试 |
| `/service-config` | service_config.html | 服务配置 |
| `/report-config` | report_config.html | 上报配置 |
| `/api-docs` | api_docs.html | API文档 |

---

## 5. 执行流程图

### 5.1 CANoe TestModule 执行流程

```
                    ┌──────────────┐
                    │  收到任务    │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ TaskCompiler │
                    │ 生成 Plan    │
                    └──────┬───────┘
                           │
                           ▼
            ┌────────────────────────────────┐
            │ ExecutionStrategySelector      │
            │ 选择 CANoeExecutionStrategy    │
            └──────────────┬─────────────────┘
                           │
                           ▼
            ┌────────────────────────────────┐
            │ prepare 阶段校验 capability    │
            │ configuration / measurement /  │
            │ test_module                    │
            └──────────────┬─────────────────┘
                           │
                           ▼
            ┌────────────────────────────────┐
            │ 加载 cfg 并启动测量             │
            └──────────────┬─────────────────┘
                           │
                           ▼
            ┌────────────────────────────────┐
            │ 逐个执行 TestModule            │
            │ execute_test_module_direct()   │
            └──────────────┬─────────────────┘
                           │
                           ▼
            ┌────────────────────────────────┐
            │ 收集结果、工件与观测数据        │
            └──────────────┬─────────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ 上报结果     │
                    └──────────────┘
```

---

## 6. 数据流

### 6.1 任务执行数据流

```
外部系统 ──POST──> task_api.py ──> task_executor_production.py
                              │
                              ├──> case_mapping_manager.py (查询映射)
                              │
                              ├──> config_manager.py (准备配置)
                              │       │
                              │       └──> config_cache_manager.py (缓存cfg)
                              │
                              ├──> AdapterFactory ──> CANoeAdapter
                              │                           │
                              │                           ├──> COMWrapper
                              │                           └──> TestEngine
                              │
                              ├──> result_collector.py (收集结果)
                              │
                              └──> report_client.py (上报结果)
```

---

## 7. 关键接口

### 7.1 用例映射接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/case-mappings` | 获取用例映射列表 |
| POST | `/api/case-mappings` | 创建用例映射 |
| PUT | `/api/case-mappings/{case_no}` | 更新用例映射 |
| DELETE | `/api/case-mappings/{case_no}` | 删除用例映射 |
| POST | `/api/case-mappings/{case_no}/enable` | 启用用例映射 |
| POST | `/api/case-mappings/{case_no}/disable` | 禁用用例映射 |
| POST | `/api/case-mappings/import/execute` | 批量导入 |
| GET | `/api/case-mappings/export` | 导出映射 |

### 7.2 任务接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/tasks` | 获取任务列表 |
| POST | `/api/tasks` | 创建任务 |
| GET | `/api/tasks/{task_id}` | 获取任务详情 |
| POST | `/api/tasks/{task_id}/execute` | 执行任务 |
| POST | `/api/tasks/{task_id}/cancel` | 取消任务 |
| DELETE | `/api/tasks/{task_id}` | 删除任务 |

---

## 8. 配置文件说明

### 8.1 config.json 结构

```json
{
  "server": {
    "websocket_url": "ws://localhost:8080/ws/executor",
    "heartbeat_interval": 30,
    "reconnect_interval": 5,
    "max_reconnect_attempts": 10
  },
  "http": {
    "port": 8180,
    "host": "0.0.0.0"
  },
  "device": {
    "device_id": "DEVICE_001",
    "device_name": "测试执行设备-01",
    "location": "实验室"
  },
  "software": {
    "canoe_path": "C:\\Program Files\\Vector\\CANoe 17\\Exec64\\CANoe64.exe",
    "tsmaster_path": "C:\\Program Files\\TSMaster",
    "ttman_path": "C:\\Spirent\\TTman.bat",
    "workspace_path": "C:\\TestWorkspace"
  },
  "logging": {
    "level": "INFO",
    "log_dir": "logs",
    "max_log_files": 10,
    "max_log_size_mb": 100
  },
  "execution": {
    "default_timeout": 3600,
    "auto_start": True,
    "keep_alive": True
  },
  "report": {
    "enabled": False,
    "api_url": "",
    "file_upload_url": ""
  },
  "config_cache": {
    "enabled": True,
    "cache_dir": "workspace/cache/configs",
    "max_cache_count": 50
  }
}
```

---

## 9. 版本信息

- **当前版本**: 1.0.0
- **创建日期**: 2026-02-25
- **更新日期**: 2026-03-20

---

## 10. 附录

### 10.1 依赖项

```
Flask>=2.0.0
pywebview>=4.0
pandas>=1.3.0
openpyxl>=3.0.0
requests>=2.25.0
```

### 10.2 打包命令

```bash
pyinstaller TestExecutor.spec
```
