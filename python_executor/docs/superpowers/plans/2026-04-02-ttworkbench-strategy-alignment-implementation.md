# TTworkbench Strategy Alignment Implementation Plan

## Scope

Align `TTworkbench` with the existing `strategy + capability + raw adapter` execution model.

## Steps

1. Add TTworkbench capability definitions and adapter registration
2. Add `TTworkbenchExecutionStrategy`
3. Extend selector and package exports
4. Add tests for selector, adapter capabilities, and strategy execution
5. Verify full pytest suite
