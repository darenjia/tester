# 回滚检查清单

## 进入回滚前

- 先停止接收新的任务入口
- 导出最近的失败报告与诊断摘要
- 记录当前 `config.json`、运行版本和最近异常时间

## 回滚动作

- 切回上一个确认可用的版本或配置
- 重启执行器
- 调用 `GET /api/runtime/preflight` 验证不再存在 `blocked`
- 调用 `GET /health` 与 `GET /api/runtime/diagnose` 验证基础功能恢复

## 回滚后确认

- 核对失败报告是否仍可查询
- 手动触发一条最小测试任务确认执行链路恢复
- 记录本次回滚原因与恢复时间
